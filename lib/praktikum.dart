import 'package:flutter/material.dart';

class MyPraktikumWidget extends StatelessWidget {
  const MyPraktikumWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
    home: Scaffold();
  }
}